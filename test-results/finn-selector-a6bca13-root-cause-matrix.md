# FINN selector a6bca13 QA root-cause matrix

Source release: `a6bca13bae934a15abeff3fd99ea873bbaf9a7a2`.

## Source note

The requested source filename `finn-selector-a6bca13-final-qa-report.md` is
not present in the repository or local QA artifacts. The corresponding
independent production report is
`test-results/finn-selector-b13b871-final-qa-report.md`; it names the deployed
candidate SHA and contains the prompts, terminal responses, run IDs, evidence,
verifier reasons, and sealed selector results used below. Published sealed
queries are regression inputs only in this repair and are not reused as holdout
evidence.

| QA reproduction / family | First failing boundary | Proven symptom | Generic root cause | Structural repair boundary | Regression proof |
| --- | --- | --- | --- | --- | --- |
| Capability versus off-topic | Structured selector contract normalization | Product capability wording can become off-topic/unavailable in runtime while weather is treated as financial unavailable. | Typed safe-terminal contracts are not normalized from the selected immutable contract before runtime state decisions. | Normalize domain, polarity, support and terminal semantic contracts centrally; never infer a financial default after selector output. | Capability and unrelated-domain semantic variants; no product tools for off-topic. |
| Active setup versus active asset | Selector manifest and operation facts | Workspace-state questions can collapse setup into selected asset reads. | State-bearing read contracts lack sufficiently separated semantic and response field descriptions. | Strengthen the manifest schema/descriptions and validate typed selected operation facts against the contract. | Active setup, asset, plan and indicator variants. |
| Elliptical reformulation and evidence follow-up | Verified lineage projection | Reformulation returns unavailable and evidence/bot follow-up repeats an evidence limitation. | The verified result retains too little answer/evidence material and follow-ups do not consume a stable lineage object. | Persist immutable verified response basis; project it into evidence, reformulate and contextual bot responses; failed/off-topic turns cannot replace it. | Evaluate -> evidence -> reformulate -> bot, with an off-topic interruption. |
| Explicit setup name and complete setup slots | Request preprocessing to typed operation state | Complete DOT setup still has missing name/symbol/type despite all slots in natural text. | Parsed semantic slots are not canonically merged with selector facts before required-input computation. | Merge only typed facts into the selected contract state and compute required inputs once. | Published DOT prompt plus semantically distinct complete setup wording. |
| Natural asset names | Asset canonicalization | Natural names, including Avalanche, can remain noncanonical or drift in later workflow stages. | Name-to-ticker canonicalization is incomplete or applied after state/proposal identity has been derived. | Canonicalize asset references before selector entity validation, state merge, target resolution and idempotency keys. | Avalanche/AVAX, Cardano/ADA and explicit target-over-workspace cases. |
| Verified EVALUATE context | Orchestrator verified-context persistence | Grounded evaluation does not reliably leave usable `last_verified_context`; later turns lose the basis. | Verified context is written only for a subset of terminal strategies or with incomplete payload. | Store a typed immutable context after every valid grounded evaluate delivery, with answer, observations, evidence and entities. | Grounded evaluation and continuity after safe terminal/off-topic turns. |
| Personal indicator response | Deterministic response construction / coverage verifier | Valid indicator evidence is rejected for missing `configured_count` and `indicator_names`. | Response evidence facts are not promoted into required contract response fields. | Construct indicator responses deterministically from canonical evidence, including truthful zero-count cases. | Explicit BTC over AAPL; empty/nonempty configurations; general concept remains tool-free. |
| Bot status response | Deterministic response construction / coverage verifier | Bot reads can have evidence but omit typed `bot` / `bot_status` response fields. | Bot status facts are left in prose rather than contract response coverage. | Construct bot status fields deterministically from canonical linked-bot evidence. | Active bot-status read and forbidden live activation safety sequence. |
| Live bot activation | Selector manifest safety contract | Natural “real orders” wording becomes unsupported with read polarity. | High-risk activation semantics compete with generic autonomous-trading unsupported semantics. | Make the supported, guarded activation proposal contract explicit and preserve its activate polarity through classification. | Published real-orders wording plus distinct live activation variants; execution remains policy-blocked. |
| Clarification semantics | Classification normalization | Ambiguous improvement request has create polarity and misses `requested_change`. | Clarify results retain provider-provided generic action metadata. | Normalize clarification to its typed update semantics and its unresolved input. | Published prompt plus distinct ambiguous workflow requests. |
| Proposal correction and idempotency | Operation state / proposal repository | ADA correction loses active proposal; repeated equivalent request can make a second draft. | Proposal identity is not a canonical, normalized contract+target+payload key and active equivalent drafts are not reused. | Derive a canonical idempotency key from normalized typed proposal input; find/reuse equivalent active draft before creation. | Cardano -> ADA correction and repeated ADA request asserting same proposal ID. |
| Guided setup | Active guided operation state | Type/timeframe slot turns downgrade or become unavailable before a draft. | Active operation context is not an authoritative constraint across short slot answers. | Bind continuation to persisted guided contract, merge one typed slot, retain target and create one proposal only at completeness. | Four-turn setup, reordered slots, retry, no write before confirmation. |
| EVALUATE usefulness and repair | Reasoning primary/repair schema | Unsupported causal claims trigger failed repair then generic evidence limitation. | Primary and repair prompts do not express supported observations or a usable noncausal answer shape precisely enough. | Tighten primary/repair schema and examples around grounded observations, limitations and next steps without relaxing causality/verifier guards. | Supported plan review, forbidden causality rejection, useful repaired response and lineage reuse. |
| Polling/SSE and release metadata | Transport serializer / deploy metadata | Transport parity is required; release marker can remain stale after accepted release. | Timestamp serialization and release-marker mutation are independent of canonical terminal/deployed state. | Keep one JSON-safe terminal serializer and update `LAST_GOOD_COMMIT` only under explicit verified-acceptance semantics. | Exact terminal envelope parity and release metadata contract checks. |

## Guardrails

- No keyword router, exact-prompt match, V1 fallback, new holdout, or verifier/safety relaxation.
- Write flows remain proposal then explicit confirmation then execution.
- Target and ownership invariants remain enforced before tools, evidence, proposals and writes.
