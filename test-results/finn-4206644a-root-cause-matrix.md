# FINN QA Repair Root-Cause Matrix

| Symptom | First concrete cause | Repair boundary | Safety risk | Regression proof |
| --- | --- | --- | --- | --- |
| `vieruursgrafiek` leaves a complete AVAX setup incomplete | The timeframe catalog only accepted detached aliases protected by word boundaries. | Canonical setup timeframe catalog before typed input reconciliation. | A write proposal could solicit already-supplied data or use an unintended timeframe. | Compound NL/EN/DE timeframe and complete AVAX setup tests. |
| BTC EVALUATE loses useful content after a rejected indicator inference | A contract-limited repair was represented as a normal repaired EVALUATE conclusion. | Evaluate response/repair/verifier contract and terminal lineage projection. | An unverified causal conclusion could be reused as trusted conversation state. | Reasoning contract, response coverage, verifier persistence and orchestrator flow tests. |
| Degraded lineage is absent after terminal persistence | The verifier reject discarded its draft provenance before the orchestrator's conversation update boundary. | Central terminalization and conversation persistence boundary. | Safe evidence follow-ups cannot distinguish a bounded result from a verified conclusion. | Rejected and contract-limited EVALUATE lineage tests in `test_finn_v2_orchestrator_flow.py`. |
| Plan-evaluation, live-automation and NEAR target regressions | The published failures were already covered by the registry-driven selector projection and regression fixtures on the candidate baseline. | Operation registry, typed selector projection and canonical asset resolver. | Wrong operation, wrong target, or unsafe activation path. | Selector classification, structured selector, target resolver and golden registry tests. |
| Validator action mismatch across read/evaluate/create/add/remove/activate | The only accepted action source is the selected OperationContract polarity; fixture expectations are validated against that source. | Registry validator, not dataset-specific routing. | A valid safe action can be rejected, or an invalid action can reach policy. | Operation classification and selector eval registry tests. |
