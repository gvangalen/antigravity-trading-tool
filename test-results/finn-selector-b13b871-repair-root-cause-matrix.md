# FINN selector b13b871 repair root-cause matrix

Source: independent production QA on `b13b871087786a9e79fc6b1d519e3df19242a0b7`.

| Reproduction | Expected vs actual operation | Entities / target | Polarity | Conversation reference | Runtime phase | First failing boundary | Generic root cause | Structural correction | Regression coverage |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| “Laat mijn gekoppelde robot voortaan echte orders plaatsen.” | `activate_bot` vs `unsupported_financial_operation` | linked bot / no explicit target | `activate` vs `read` | none | selector | structured selector contract semantics | Live execution intent is not distinguished strongly enough from unsupported autonomous management and bot-status/configuration reads. | Encode typed semantic distinctions and action constraints in the operation manifest and validate the selected contract/polarity as one result. | Published failure plus semantically different real-order, bot-status and bot-config variants. |
| Complete DOT setup sentence | `create_setup` vs `create_setup` | DOT correct; natural name/type/timeframe not projected | `create` correct | none | classification validation | selector entity/slot projection | The selector schema only carries generic entity IDs; setup slots extracted by preprocessing are not merged into the selected operation state before missing-input calculation. | Canonically merge validated deterministic facts and structured selection into typed operation inputs, then compute missing inputs from the contract once. | Complete one-turn DOT setup and alternate asset/type/timeframe/name formulations. |
| Ambiguous “Maak mijn manier van handelen beter.” | `clarify_request` vs same | no target | `update` vs `create` | none | selector/classification | clarification normalization | Clarification inherits a model-chosen generic polarity and empty missing inputs instead of the contract’s ambiguity semantics. | Normalize clarification action to typed update intent and persist `requested_change` as the unresolved input. | Published phrase plus semantically distinct ambiguous improvement requests. |
| ATR explanation | correct operation | expected canonical `ATR`; actual expanded label | `read` correct | none | eval projection | typed entity normalization | Financial concept values are not canonicalized before typed comparison/propagation. | Canonicalize known concept identifiers while preserving display labels separately. | ATR expanded-name regression and RSI/MACD controls. |
| BTC guided setup: start → type → timeframe → name | `create_setup` continuation becomes `clarify_request`/unavailable before eventual proposal | BTC and collected slots intermittently lost | create/update drift | active guided operation not consistently applied | conversation state | persisted operation-state merge before selector | Short slot answers are reclassified against the full manifest and failed turns can interrupt active guided state. | Bind turns to the persisted active operation, merge exactly one slot per turn, retain target/contract/entities, and emit exactly one proposal after completeness. | Full four-turn sequence, alternative slot order, retry and single-proposal invariants. |
| EVALUATE → evidence → reformulate → bot implication | evidence/reformulation contracts selected inconsistently; bot follow-up becomes generic evidence response | prior plan entities/evidence not reused in response | reads/evaluate drift | previous verified response not reliably consumed | lineage/reasoning | verified-context projection into follow-up response strategy | Last verified lineage stores insufficient response/evidence material or follow-up services ignore it; failed turns interfere with usable lineage. | Persist immutable last verified financial basis and project its conclusion/evidence into evidence, reformulation and contextual bot contracts; failed/off-topic turns cannot replace it. | Four-turn sequence plus off-topic interruption and failed-turn retention. |
| General RSI vs personal BTC RSI | concept read passes; personal read rejected | BTC target and evidence correct, configured fields omitted from response coverage | read correct | same conversation | deterministic response/verifier | response construction before coverage verifier | Indicator reasoning states facts in prose but does not populate required semantic response fields `configured_count` and `indicator_names`. | Build typed indicator response fields directly from canonical evidence and expose them to response coverage/verifier. | General-vs-personal sequence, zero/nonzero configs, explicit BTC over stale AAPL. |
| AAPL workspace + explicit Bitcoin indicator read | correct BTC operation/tool target, verifier rejects; production fixture returned zero configs | BTC precedence correct | read correct | none | evidence/response | canonical indicator evidence and response projection | Target precedence works, but empty canonical fixture evidence plus omitted response fields makes the read unusable. | Preserve explicit-target invariant and make zero/nonzero canonical results verifier-complete without inventing records. | Explicit BTC over AAPL, owner/symbol provenance and empty-result response contract. |
| Financial → off-topic → evidence resume | weather turn becomes financial `UNAVAILABLE`; resume downgrades | no product entities for weather | read | prior financial reference should survive | selector/context | off-topic classification and lineage update | Runtime selector/context path diverges from selector-only behavior and unavailable/off-topic turns affect follow-up lineage. | Apply the same structured operation contract in runtime and update last verified financial context only after a verified financial delivery. | Full interrupt sequence and no-tools/no-overwrite invariants. |
| Plan evaluation reasoning | `evaluate_plan` correct | eight scopes/tools/evidence complete | evaluate correct | basis for follow-ups | model reasoning/repair | causal-claim verifier repair boundary | Model emits unsupported causality; repair repeats invalid claims and falls back to generic evidence limitation. | Supply explicit allowed inference forms and evidence-field semantics to primary/repair contracts; require repair to answer the question using supported observations without weakening verifier rules. | Complete-plan reasoning fixtures, forbidden causality, successful supported repair and lineage reuse. |
| Watchlist ADA target correction | proposal is created, follow-up target correction becomes unavailable | ADA proposal target initially correct | add | active proposal/guided state lost | conversation/workflow | active operation/proposal continuation | A short correction is treated as a fresh ambiguous request instead of an update to persisted operation state. | Bind correction to open proposal/operation state, revalidate payload and retain no-write-before-confirmation. | Cardano→ADA correction, proposal uniqueness, confirmation/execution/idempotency. |

## Invariants retained

- No V1 fallback.
- No keyword or regex router and no exact prompt matching.
- Verifier, ownership, asset isolation, confirmation and live-action safety remain strict.
- No business write before a typed proposal and explicit confirmation.
- Published QA prompts become regression data only; Build creates no new sealed holdout.
