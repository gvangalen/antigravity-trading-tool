# FINN selector a29089df root-cause matrix

Source: independent QA report `finn-selector-a29089df-final-report.md`.

| Boundary | Reproductions | First failing boundary | Generic root cause | Corrective direction | Regression coverage |
| --- | --- | --- | --- | --- | --- |
| Operation selection | capability, plan review, setup draft, personal indicators, live bot, unsupported finance | structured-selector manifest/instructions | Contract descriptions do not make the semantic distinctions and required state transitions sufficiently explicit; low confidence becomes `unavailable` rather than a typed selected contract. | Strengthen manifest semantics and validate selected contracts without a local alternate router. | Public QA prompts become regression cases; selector contract tests cover semantic families. |
| Target extraction | explicit BTC indicator read, Cardano/ADA watchlist | request plan / tool-plan projection | `target_asset` is only populated for watchlist operations and is reconstructed separately from selector facts. | One precedence resolver: current explicit target, verified lineage, persisted guided/proposal target, workspace fallback. Preserve `context_asset` separately. | Explicit BTC/ADA against stale AAPL/BTC context through selector, plan, tools, evidence and proposals. |
| Action polarity | watchlist add/remove and activate bot | classification validation / request plan | Selector output does not carry a canonical action; facts and contract polarity are compared but delivery can receive a divergent default. | Resolve polarity once from validated contract plus user action facts, store it in plan/state/proposal/execution and reject inconsistencies before tools. | Add/remove/create/activate and read no-proposal invariants. |
| Guided setup | four-turn BTC setup | selector-to-active-guided state handoff | A short slot answer is still sent through an unconstrained full manifest and can select `clarify_request`; request state therefore cannot advance. | Bind a persisted active guided contract as a selector constraint, merge slots, recompute missing inputs and draft exactly once. | Full BTC setup sequence, retry/idempotency tests. |
| Watchlist | Cardano then ADA correction | target propagation into tool/evidence plan | Plan has ADA but tool selector/evidence projection use workspace BTC and a mismatched payload type. | Feed canonical resolved target into every tool selector and evidence artifact; enforce asset invariant before proposal/write. | ADA watchlist sequence plus separate remove polarity. |
| Evaluation lineage | evaluate, evidence, reformulate, bot follow-up | verified lineage / response verifier | Follow-up contracts lose the prior verified response or expected response fields; a failed turn then appears as generic unavailable. | Persist canonical verified lineage and require it in tool/reasoning/verifier coverage without allowing failed turns to replace it. | Four-turn evaluation sequence and context-retention tests. |
| Personal indicator read | RSI config for BTC | response coverage/verifier | Personal indicator evidence is present but required response fields are not projected into the response contract. | Align indicator evidence schema, response coverage and response construction for the resolved target. | General RSI vs stored RSI configuration, explicit BTC test. |
| Eval telemetry | all selector evals | eval result serialization | `missing_inputs` is parsed but discarded before result/report generation. | Store expected and actual values and grade absence explicitly. | Registry/runner serialization and metric tests. |
| Delivery parity | polling/SSE terminal envelopes | transport serializer | Equivalent envelopes serialize timestamps through different formatters. | Canonical UTC ISO-8601 serializer shared by polling and SSE. | Exact byte-equivalent terminal envelope test. |
| Release metadata | live a290 vs LAST_GOOD 48fb | deploy metadata update | Deploy marker is updated independently of verified deployed SHA and can mark an unaccepted release as good. | Tie active/backend/frontend/deployed SHA checks to release metadata; update last-good only with explicit QA-accepted input. | Release metadata mismatch contract test. |

The published sealed and QA holdout prompts are regression inputs only. They are not reused as independent holdout evidence for this candidate.
