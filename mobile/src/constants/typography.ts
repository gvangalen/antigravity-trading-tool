import type { TextStyle } from 'react-native';

export const typography = {
  eyebrow: {
    fontSize: 11,
    fontWeight: '800',
    letterSpacing: 1.6,
    lineHeight: 14,
    textTransform: 'uppercase',
  } satisfies TextStyle,
  sectionTitle: {
    fontSize: 14.5,
    fontWeight: '800',
    letterSpacing: -0.3,
    lineHeight: 18,
  } satisfies TextStyle,
  heroTitle: {
    fontSize: 16,
    fontWeight: '900',
    letterSpacing: -0.4,
    lineHeight: 20,
  } satisfies TextStyle,
  displayValue: {
    fontSize: 17,
    fontWeight: '900',
    letterSpacing: -0.6,
    lineHeight: 21,
  } satisfies TextStyle,
  metricValue: {
    fontSize: 14,
    fontWeight: '800',
    lineHeight: 17,
  } satisfies TextStyle,
  cardTitle: {
    fontSize: 13,
    fontWeight: '700',
    letterSpacing: -0.2,
    lineHeight: 16,
  } satisfies TextStyle,
  bodyLarge: {
    fontSize: 11,
    fontWeight: '500',
    lineHeight: 15,
  } satisfies TextStyle,
  body: {
    fontSize: 12,
    fontWeight: '400',
    lineHeight: 16,
  } satisfies TextStyle,
  bodyStrong: {
    fontSize: 12,
    fontWeight: '500',
    lineHeight: 16,
  } satisfies TextStyle,
  meta: {
    fontSize: 10,
    fontWeight: '500',
    lineHeight: 13,
  } satisfies TextStyle,
  metaStrong: {
    fontSize: 10,
    fontWeight: '700',
    lineHeight: 13,
  } satisfies TextStyle,
  subcopy: {
    fontSize: 10.5,
    fontWeight: '500',
    lineHeight: 14,
  } satisfies TextStyle,
  action: {
    fontSize: 12.5,
    fontWeight: '700',
    lineHeight: 15,
  } satisfies TextStyle,
  actionStrong: {
    fontSize: 12.5,
    fontWeight: '800',
    lineHeight: 15,
  } satisfies TextStyle,
  listRowTitle: {
    fontSize: 11,
    fontWeight: '800',
    lineHeight: 13.5,
  } satisfies TextStyle,
  listRowMeta: {
    fontSize: 9,
    fontWeight: '500',
    lineHeight: 12,
  } satisfies TextStyle,
  listRowStatus: {
    fontSize: 8,
    fontWeight: '800',
    lineHeight: 9.5,
  } satisfies TextStyle,
  listRowAction: {
    fontSize: 8,
    fontWeight: '700',
    lineHeight: 9.5,
  } satisfies TextStyle,
  chipLabel: {
    fontSize: 10,
    fontWeight: '800',
    letterSpacing: 0.8,
    lineHeight: 12,
    textTransform: 'uppercase',
  } satisfies TextStyle,
  chipLabelCompact: {
    fontSize: 8,
    fontWeight: '800',
    letterSpacing: 0.8,
    lineHeight: 9.5,
    textTransform: 'uppercase',
  } satisfies TextStyle,
  metricLabel: {
    fontSize: 9.5,
    fontWeight: '800',
    letterSpacing: 0.9,
    lineHeight: 11,
    textTransform: 'uppercase',
  } satisfies TextStyle,
  metricLabelStrong: {
    fontSize: 9.5,
    fontWeight: '900',
    letterSpacing: 1.1,
    lineHeight: 11,
    textTransform: 'uppercase',
  } satisfies TextStyle,
};
